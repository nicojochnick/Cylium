import React, {Component} from 'react';
import PropTypes from 'prop-types';

class Url extends Component {
    render() {
        return (
            <div>

            </div>
        );
    }
}

Url.propTypes = {};

export default Url;
